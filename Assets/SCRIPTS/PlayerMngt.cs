using UnityEngine;



public enum Character
{
    Blank,
    Chicken,
    Deer,
    Dog,
    Horse,
    Kitty,
    Pinguin,
    Tiger,

}
public class PlayerMngt : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
